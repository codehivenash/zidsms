import Ember from 'ember';
import Faker from 'faker';

let groupTypes = ['Corporate', 'Diploma', 'Certificate', 'Other'];
let groupStatus = ['Open', 'In Progress','Closed'];

export default Ember.Controller.extend({
	title: ''
});